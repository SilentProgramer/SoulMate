import {Component} from '@angular/core';        //path is resolved in systemjs.config.js

@Component({
    selector:'nav-bar',
    templateUrl: '../components/navbar/nav-bar.html'    //path is resolved in server.js
})

export class NavBarComponent{  
}